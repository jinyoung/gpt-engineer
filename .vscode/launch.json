{
    "version": "0.1.0",
    "configurations": [
        {
            "name": "my-project",
            "type": "python",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "module": "gpt_engineer.applications.cli.main",
            "args": ["projects/example"],// "-sh"],
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}